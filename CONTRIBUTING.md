# 贡献与维护

RabbitMQ Guard 使用短分支、Pull Request 和自动化检查维护。`main` 应当始终可运行。

## 基本流程

1. 从最新 `main` 创建短期分支，分支名使用 `codex/`、`fix/`、`feature/` 或 `docs/` 前缀。
2. 保持改动聚焦；诊断规则、数据格式和安全边界不要混在同一个 PR 中。
3. 本地运行 `make verify` 和 `make smoke`。
4. 创建 PR，填写风险、验证、review 重点和回滚方案。
5. 等待 required checks 通过，再完成 review 和 squash merge。
6. 合并后删除分支；发布行为变化时更新 `CHANGELOG.md`。

禁止直接向 `main` 推送日常改动。初始仓库引导提交除外。

## 与风险成比例的 Review

### 低风险

文档、拼写和不改变规则标签的合成案例说明。自动检查通过后，可以快速 review 并合并。

### 中风险

诊断阈值、规则、Management API 字段、Web API、SQLite 和界面交互。必须包含自动测试；review 需要核对误报、向后兼容和报告证据。

### 高风险

凭据、外部网络访问、客户数据、权限、公开部署或任何自动修复能力。除了自动检查，还需要明确威胁模型、手工集成验证和回滚步骤；有第二位维护者后必须由非作者批准。

## 单人维护阶段

当前阶段仍然执行 PR 和 diff review，但不在 GitHub 上强制第二个人批准。作者不能用“CI 通过”代替代码审查：合并前至少检查行为变化、失败路径、日志内容和安全边界。

当满足任一条件时，将 main 规则提高为至少 1 个非作者批准：

- 出现第二位长期维护者
- 接入第一位生产客户
- 存储真实客户数据
- 引入付费、账号或自动化生产操作

## 验证命令

```bash
make verify
make smoke
make integration
```

`make integration` 需要本地实验 RabbitMQ 正在 `http://localhost:15673` 运行。GitHub 中的真实 RabbitMQ 集成测试每周运行，也可以手动触发。

## Review 判断标准

优先发现：

1. 会造成错误诊断、漏报或误报的行为变化。
2. 密码、连接信息和客户数据泄漏。
3. 对非回环地址开放实时连接或扩大 SSRF 风险。
4. 破坏已有快照格式、报告格式或历史记录的兼容性。
5. 没有真实需求证据的范围扩张。
